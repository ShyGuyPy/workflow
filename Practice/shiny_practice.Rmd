---
title: "Shiny_practice"
author: "Luke Vawter"
date: "September 12, 2018"
output: html_document
---

a space for practicing basic shiny app elements, features and ...well.. practices:

isolate(input$value...) makes input non-reactive

observeEvent

test of Render UI
use explained in tutorial https://www.youtube.com/watch?v=JUop-YfRAuw:
```{r}
library(shiny)

ui <- fluidPage(
  title = "test",
  selectInput(inputId = "thing", label = "choose a color", choices = c("red", "orange", "yellow") ),
  uiOutput("first")
  
)

server <- function(input, output)({
  var <- reactive({case_when(
    value <- input$thing == "red" ~ "apples",
    value <- input$thing == "orange" ~ "oranges",
    value <- input$thing == "yellow" ~ "banannas"
  )
  })
  output$first <- renderUI({
    selectInput("variablex", "select 1st var", choice = var())
  })
  
})



shinyApp(ui, server)
```

pulled from stack-overflow:
```{r}
library(shiny)

ui <- fluidPage(
  selectInput("num", "Choose a number", 1:10),
  conditionalPanel(
    condition = "output.square",
    "That's a perfect square!"
  )
)

server <- function(input, output, session) {
  output$square <- reactive({
    sqrt(as.numeric(input$num)) %% 1 == 0
  })
  outputOptions(output, 'square', suspendWhenHidden = FALSE)
}

shinyApp(ui = ui, server = server)

```

```{r}
library(shiny)

ui <- dashboardPage(
  dashboardHeader(title = "break me"
                  ),
  dashboardSidebar(dateInput("enter a date: ")
                   ),
  dashboardBody()
)
  
server <- function(input, output, session) {
htmlOutput(.....)
}

shinyApp(ui = ui, server = server)

```

